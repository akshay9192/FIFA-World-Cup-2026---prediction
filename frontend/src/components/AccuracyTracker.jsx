import React from 'react';
export default function AccuracyTracker({ accuracy }) { const value = accuracy?.accuracy_percentage || 0; const cls = value >= 60 ? 'bg-emerald-500' : value >= 40 ? 'bg-amber-400' : 'bg-rose-500'; return <div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="text-sm text-slate-400">Model accuracy</div><div className="mt-2 flex items-end gap-3"><span className="text-4xl font-black">{value}%</span><span className={`rounded-full px-3 py-1 text-xs font-bold text-slate-950 ${cls}`}>{accuracy?.correct || 0}/{accuracy?.total_predictions || 0}</span></div></div>; }
