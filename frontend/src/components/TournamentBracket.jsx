import React from 'react';
export default function TournamentBracket({ teams = [] }) { const rounds = [['R32', teams.slice(0, 8)], ['R16', teams.slice(0, 4)], ['QF', teams.slice(0, 4)], ['SF', teams.slice(0, 2)], ['Final', teams.slice(0, 2)]]; return <div className="grid grid-cols-5 gap-4">{rounds.map(([round, items]) => <div key={round}><h3 className="mb-3 font-bold text-emerald-300">{round}</h3><div className="space-y-3">{items.map(t => <div key={`${round}-${t.team}`} className="rounded-xl bg-slate-900 p-3 text-sm"><b>{t.team}</b><div className="text-slate-400">Win {(t.win_probability*100).toFixed(1)}%</div></div>)}</div></div>)}</div>; }
