import React, { useEffect, useState } from 'react';
import { Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { api } from '../api';
import AccuracyTracker from '../components/AccuracyTracker';

export default function Accuracy() { const [accuracy, setAccuracy] = useState(null); const [bias, setBias] = useState(null); useEffect(() => { api('/accuracy').then(setAccuracy); api('/bias').then(setBias); }, []); return <div className="space-y-6"><h1 className="text-4xl font-black">Accuracy</h1><div className="grid grid-cols-3 gap-6"><AccuracyTracker accuracy={accuracy}/><div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="text-sm text-slate-400">Group stage</div><b className="text-3xl">{accuracy?.group_stage_accuracy || 0}%</b></div><div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><div className="text-sm text-slate-400">Knockouts</div><b className="text-3xl">{accuracy?.knockout_accuracy || 0}%</b></div></div><div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="mb-4 text-xl font-bold">Running accuracy</h2><ResponsiveContainer width="100%" height={300}><LineChart data={accuracy?.history || []}><XAxis dataKey="date" stroke="#94a3b8"/><YAxis stroke="#94a3b8"/><Tooltip/><Line type="monotone" dataKey="accuracy" stroke="#34d399" strokeWidth={3}/></LineChart></ResponsiveContainer></div><div className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="text-xl font-bold">User vs model</h2><p className="mt-2 text-slate-300">{bias?.leader || 'tied'} leads by {bias?.margin || 0}. User {bias?.user_correct || 0} — Model {bias?.model_correct || 0}</p></div></div>; }
